# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*.db
backend/venv/

# Secrets — never commit ( .env matches both the root .env and backend/.env )
.env
# A bare `.env` does not match `.env.backup-before-pooler`, `.env.local`, `.env.bak`
# and the like — gitignore patterns are literal, not prefixes. One of those was sitting
# untracked in backend/ with 82 lines of live credentials in it (DATABASE_URL, and the
# Gemini / OpenAI / OpenRouter / Firecrawl / Tavily keys), one `git add -A` away from
# being published. Cover the whole family.
.env.*
# ...but keep the safe placeholder file
!.env.example

# Python compiled files
__pycache__/
*.pyc

# Generated activity export (contains user data)
activity_log.csv
.vercel

# Runtime output from the creative pipeline - regenerated per video, not source
backend/generated_media/

# Local safety copy taken before the finaldashboard merge — not part of the project
.backup-before-pull-*/

# Local editor config
.claude/

# Local copy-sheet of deployment secrets — never commit
RENDER-ENV-VALUES.txt

# Local .env backups — these hold real credentials and must never be committed.
backend/.env.bak-*
.env.bak-*

# uv/modern virtualenvs. Only `backend/venv/` was listed, so `backend/.venv/` (what uv
# creates) was tracked - that is ~2.5GB of torch and CUDA wheels.
.venv/
backend/.venv/

# The nested clone of this same repo (used to fetch the branch).
raftra/

.vercel
